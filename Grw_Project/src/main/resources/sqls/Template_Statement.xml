<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.rgb.grw.dao.TemplateDaoImpl">

<select id="selectTemplateList" resultType="TemplatePreviewDto">
	SELECT TEMP_ID, TEMP_TITLE, TEMP_CONTENT, IMG_VALUE, TO_CHAR(TEMP_REGDATE, 'YYYY.MM.DD') TEMP_REGDATE 
	FROM TEMPLATE t
</select>

<insert id="writeTemplate" parameterType="TemplatePreviewDto">
    INSERT INTO TEMPLATE (TEMP_ID, TEMP_TITLE,
                          TEMP_CONTENT, TEMP_REGDATE, IMG_VALUE)
    VALUES (temp_id_seq.NEXTVAL, 
            #{temp_title, jdbcType=VARCHAR},
            #{temp_content, jdbcType=CLOB},
            #{temp_regdate, jdbcType=DATE},
            #{img_value, jdbcType=VARCHAR})
</insert>

</mapper>
